// require('dotenv').config();

const config = {
  tronMainnet: 'https://api.trongrid.io/wallet',
  shastaTestnet: 'https://api.shasta.trongrid.io/wallet',
  nileTestnet: 'https://nile.trongrid.io/wallet',
};

module.exports = config;
